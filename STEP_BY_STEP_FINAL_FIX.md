# Пошаговое исправление проблемы с типом user_role

## 🎯 Проблема:
PostgreSQL не может сравнивать `user_role` с `text` даже в CASE выражении при изменении типа колонки.

## ⚡ Решение - пошаговый подход:

### Шаг 1: Исправьте структуру таблицы
Выполните в Supabase Dashboard → SQL Editor:
```sql
-- Содержимое файла database/fix_role_step_by_step.sql
```

**Что делает этот скрипт:**
1. ✅ Создает enum тип `user_role`
2. ✅ Добавляет новую колонку `role_new` с правильным типом
3. ✅ Копирует данные из старой колонки в новую с преобразованием типов
4. ✅ Удаляет старую колонку и переименовывает новую
5. ✅ Проверяет результат

### Шаг 2: Пересоздайте функции
Выполните в Supabase Dashboard → SQL Editor:
```sql
-- Содержимое файла database/recreate_functions_after_fix.sql
```

**Что делает этот скрипт:**
1. ✅ Пересоздает `create_local_user` с правильными типами
2. ✅ Пересоздает `handle_new_user` для триггера
3. ✅ Пересоздает `sync_auth_users_to_users_table` для синхронизации
4. ✅ Восстанавливает триггер на auth.users
5. ✅ Тестирует все функции

### Шаг 3: Протестируйте в интерфейсе
1. **Попробуйте создать пользователя** через интерфейс
2. **Попробуйте синхронизацию** существующих пользователей
3. **Проверьте список пользователей**

## 🔧 Ключевое отличие нового подхода:

### Вместо изменения типа колонки:
```sql
-- НЕ РАБОТАЕТ:
ALTER TABLE users ALTER COLUMN role TYPE user_role USING role::user_role;
```

### Используем пошаговую замену:
```sql
-- РАБОТАЕТ:
1. ADD COLUMN role_new user_role;
2. UPDATE users SET role_new = CASE WHEN role::text = 'admin' THEN 'admin'::user_role ...
3. DROP COLUMN role;
4. RENAME COLUMN role_new TO role;
```

## 📊 Ожидаемые результаты:

### После первого скрипта:
```
NOTICE: Тип user_role создан
-- Показывает структуру с правильным типом колонки role
```

### После второго скрипта:
```
NOTICE: Локальный пользователь создан: final_test@example.com
-- Показывает результат синхронизации
```

### В интерфейсе:
- ✅ Создание пользователей работает без ошибок типов
- ✅ Синхронизация находит пользователей из auth.users
- ✅ Все функции работают корректно

## 🚀 После исправления:

✅ **Создание пользователей** работает полностью  
✅ **Синхронизация** находит существующих пользователей  
✅ **Триггеры** автоматически добавляют новых пользователей  
✅ **Система готова** к полному тестированию  

## 🎯 Следующие шаги:

1. **Выполните оба SQL скрипта** по порядку
2. **Протестируйте создание пользователя** в интерфейсе
3. **Протестируйте синхронизацию** существующих пользователей
4. **Начните полное тестирование** системы!

**Этот подход должен решить проблему окончательно!** 🔧✨