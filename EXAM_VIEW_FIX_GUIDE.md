# Исправление проблемы с представлением exam_status_view

## 🚨 Проблема:
Ошибка `column exam_status_view.employee_id does not exist` означает, что в представлении отсутствует колонка `employee_id`, которая нужна для фильтрации экзаменов по работнику.

## ⚡ Решение:

### Шаг 1: Исправьте представление
Выполните в Supabase Dashboard → SQL Editor:
```sql
-- Содержимое файла database/fix_exam_status_view.sql
```

**Что исправляет:**
- ✅ Добавляет отсутствующую колонку `employee_id`
- ✅ Пересоздает представление с правильной структурой
- ✅ Сохраняет все остальные колонки и логику

### Шаг 2: Протестируйте
После выполнения скрипта:
1. **Попробуйте открыть экзамены** работника
2. **Должно работать** без ошибок
3. **Проверьте отображение** статусов и дат

## 🔧 Что было исправлено:

### В представлении добавлено:
```sql
ee.employee_id,  -- КРИТИЧНО: добавлена отсутствующая колонка
```

### В компоненте добавлено:
- ✅ **Явное указание** всех нужных колонок в SELECT
- ✅ **Альтернативный способ** загрузки через прямой запрос
- ✅ **Обработка ошибок** представления

## 📊 Структура исправленного представления:

```sql
SELECT 
    ee.id,
    ee.employee_id,     -- ← ДОБАВЛЕНО
    ee.exam_id,
    e.full_name as employee_name,
    ex.name as exam_name,
    ee.exam_date,
    ee.next_exam_date,
    ee.pending_date,
    s.name as section_name,
    pt.name as profession_name,
    -- статус и цветовая индикация
    CASE ... END as status,
    CASE ... END as color_indicator
FROM employee_exams ee
JOIN employees e ON e.id = ee.employee_id
-- остальные JOIN'ы
```

## 🎯 Ожидаемый результат:

### После исправления:
- ✅ **Кнопка "Экзамены"** работает без ошибок
- ✅ **Модальное окно** открывается и показывает экзамены
- ✅ **Можно устанавливать** даты экзаменов
- ✅ **Статусы отображаются** корректно

### Fallback механизм:
Если представление все еще не работает, компонент автоматически переключится на прямой запрос к таблицам.

## 🚀 Дополнительная диагностика:

### Проверка представления:
```sql
-- Проверить структуру
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'exam_status_view';

-- Проверить данные
SELECT employee_id, employee_name, exam_name 
FROM exam_status_view LIMIT 3;
```

### Если проблемы остаются:
1. Убедитесь, что представление пересоздано
2. Проверьте права доступа к представлению
3. Посмотрите логи в консоли браузера

**Выполните SQL скрипт и попробуйте открыть экзамены работника!** 🔧